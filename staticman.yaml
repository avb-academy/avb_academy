name: "AVB Academy"
moderation: true
honeypot: website
branch: comments
path: "data/comments/{options.slug}"

allowedFields:
  - name
  - message

requiredFields:
  - name
  - message

format: "yaml"
generatedFields:
  date:
    type: date
    options:
      format: iso8601
